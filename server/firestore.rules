rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client SDK access. The backend uses Application Default Credentials
    // with the runtime service account, which bypasses rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
